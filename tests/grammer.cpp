#include <bits/stdc++.h>
using namespace std;

// func
void func(int n) {
    //
}

int main() {
    int N = 10;
    vector<int> A(N);

    // fori
    for (int i = 0; i < N; i++) {
        //
    }

    // forv
    for (int v : A) {
        //
    }

    // func_lambda
    auto lambda = [&](int a) -> void {
        //
    };

    // func_recursive
    auto recursive = [&](auto self, int a) -> void {
        self(self, a); // recursive
    };

    //recursive(recursive, 3);
}
